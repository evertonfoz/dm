// Stub implementation for platforms that don't support `dart:io` (web).

/// Empty environment map for web builds where `dart:io` is unavailable.
final Map<String, String> platformEnvironment = const {};
