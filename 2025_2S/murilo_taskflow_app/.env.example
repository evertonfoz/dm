# Example Supabase environment variables (safe to commit)
# Copy this file to `.env` and fill the real values before running locally.

# Example values (NON-FUNCTIONAL placeholders):
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_KEY="YOUR_SERVICE_ROLE_KEY_OR_ANON_HERE"
SUPABASE_ANON_KEY="YOUR_ANON_KEY_HERE"

# Notes:
# - Never commit real secret keys. Use `.env` for local development and
#   configure production secrets via your deployment platform (e.g., Vercel, Netlify, GitHub Actions secrets).
# - If your app requires a SERVICE_ROLE key, keep it server-side only.
