```markdown
# Lesson — Splashscreen sincronizada com LaunchScreen

1. Explique a diferença entre LaunchScreen (nativo) e a tela inicial Flutter.
2. Mostre como manter consistência entre ambos reutilizando assets em `assets/images/splashscreen/`.
3. Cole o snippet em `lib/features/splashscreen/splashscreen_page.dart` e execute.

Discussão: por quanto tempo devemos manter a splash visível? O que não fazer na splash (bloquear a UI, perform heavy IO).
```