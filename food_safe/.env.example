# Exemplo de variáveis de ambiente para desenvolvimento
# Copie este arquivo para `.env` e preencha com valores reais.

# Example environment variables. COPY this file to `.env` and fill the values locally.
# IMPORTANT: never commit `.env` or real secret values to version control.

SUPABASE_URL="https://your-supabase-url"
# Public Anon key or service key depending on the usage. Keep secrets out of the repo.
SUPABASE_KEY="your-supabase-key"
API_BASE_URL="https://api.example.com"
ENVIRONMENT=development
